android {
  productFlavors {
    "nativescript-grid-view" {
      dimension "nativescript-grid-view"
    }
  }
}
dependencies {
    def supportVer = project.hasProperty("supportVersion") ? supportVersion : "+";
    compile 'com.android.support:recyclerview-v7:$supportVer'
}